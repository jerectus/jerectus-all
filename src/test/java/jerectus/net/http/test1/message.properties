model.name=名前
model.num=数
jerectus.validation.Required=${name}: 入力してください
jerectus.validation.Length=\
    ${name}: <%if ($C.min() != 0) {%>${$C.min()}桁以上<%}%>\
    <%if ($C.max() != -1) {%>${$C.max()}桁以下<%}%>で入力してください
